services:
  opc-bridge:
    build: .
    image: opc-da-to-ua-bridge:dev
    ports:
      - "4840:4840"
      - "8080:8080"
    environment:
      DOTNET_ENVIRONMENT: Development
    volumes:
      - ./pki:/app/pki
