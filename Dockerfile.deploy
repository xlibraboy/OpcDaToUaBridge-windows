FROM mcr.microsoft.com/dotnet/sdk:8.0 AS build
WORKDIR /src
COPY . .
RUN dotnet publish src/OpcBridge.App/OpcBridge.App.csproj -c Release -r win-x64 --self-contained -o /out

FROM scratch AS out
COPY --from=build /out /
